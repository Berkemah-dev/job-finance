@extends('layouts.app')
@section('title','Job Order')
@section('content')
<div class="page-heading"><div><p class="eyebrow">OPERASIONAL</p><h1>Job Order</h1><p>Pantau status dan perjalanan setiap pekerjaan.</p></div>@can('quotations.manage')<a class="button button-primary" href="{{ route('quotations.index') }}">Lihat quotation</a>@endcan</div>
<section class="panel"><form method="GET" class="filter-bar"><input name="search" value="{{ $search }}" placeholder="Cari nomor atau nama pekerjaan" aria-label="Cari job"><select name="status" aria-label="Status job"><option value="">Semua status</option>@foreach(config('operations.job_statuses') as $value=>$label)<option value="{{ $value }}" @selected(request('status')===$value)>{{ $label }}</option>@endforeach</select><button class="button button-primary">Cari</button><a class="text-link" href="{{ route('jobs.index') }}">Reset</a></form>
<div class="table-scroll"><table><thead><tr><th>Nomor Job</th><th>Customer</th><th>Pekerjaan</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($jobs as $job)<tr><td>{{ $job->number }}</td><td>{{ $job->quotation_snapshot['customer']['name'] }}</td><td>{{ Str::limit($job->subject,40) }}</td><td><span class="status-badge status-{{ $job->status }}">{{ config('operations.job_statuses.'.$job->status) }}</span></td><td><a class="text-link" href="{{ route('jobs.show',$job) }}">Lihat detail</a></td></tr>@empty<tr><td colspan="5"><div class="empty-state"><h3>Belum ada Job Order yang sesuai</h3><p>Job tersedia setelah quotation disetujui dan dikonversi.</p></div></td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $jobs->links() }}</div></section>
@endsection
