@extends('layouts.app') @section('title','Jurnal Penyesuaian') @section('content')
<div class="page-heading"><div><p class="eyebrow">JURNAL MANUAL</p><h1>Jurnal penyesuaian</h1><p>Jurnal langsung diposting dan hanya dapat dikoreksi melalui reversal.</p></div><a class="text-link" href="{{ route('journals.index') }}">Kembali</a></div>
<section class="panel form-panel"><form class="data-form" method="POST" action="{{ route('journals.store') }}" data-journal-form>@csrf<div class="form-grid"><div class="field"><label>Tanggal jurnal</label><input type="date" name="journal_date" value="{{ old('journal_date',today()->toDateString()) }}" max="{{ today()->toDateString() }}" required></div><div class="field"><label>Keterangan</label><input name="description" maxlength="255" value="{{ old('description') }}" required></div></div><div class="section-heading"><h2>Debit dan kredit</h2><button type="button" class="button button-secondary button-small" data-add-entry>+ Tambah baris</button></div><div data-entry-list></div><template data-entry-template><div class="journal-entry-row"><select data-name="account_id" required><option value="">Pilih akun</option>@foreach($accounts as $account)<option value="{{ $account->id }}">{{ $account->code }} — {{ $account->name }}</option>@endforeach</select><input data-name="description" placeholder="Uraian" maxlength="255" required><input data-name="debit" type="number" min="0" step=".01" placeholder="Debit"><input data-name="credit" type="number" min="0" step=".01" placeholder="Kredit"><button type="button" class="icon-button" data-remove-entry aria-label="Hapus baris">×</button></div></template><div class="journal-total"><span>Total debit <strong data-total-debit>Rp 0,00</strong></span><span>Total kredit <strong data-total-credit>Rp 0,00</strong></span></div><div class="form-actions"><button class="button button-primary">Posting jurnal</button></div></form></section>
@endsection
