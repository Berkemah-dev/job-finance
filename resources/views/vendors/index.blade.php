@extends('layouts.app')
@section('title','Vendor')
@section('content')
<div class="page-heading"><div><p class="eyebrow">MASTER DATA</p><h1>Vendor</h1><p>Kelola Shipping Lines, Vendor Trucking, dan agent.</p></div><a class="button button-primary" href="{{ route('vendors.create') }}">+ Tambah vendor</a></div>
<section class="panel">
<form class="filter-bar" method="GET"><input name="search" value="{{ $search }}" placeholder="Cari nama, kode, atau email" aria-label="Cari vendor"><select name="archived" aria-label="Status vendor"><option value="0">Vendor aktif</option><option value="1" @selected(request('archived')==='1')>Diarsipkan</option></select><button class="button button-primary">Cari</button><a class="text-link" href="{{ route('vendors.index') }}">Reset</a></form>
<div class="table-scroll"><table><thead><tr><th>Kode</th><th>Vendor</th><th>Kategori</th><th>Kontak</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($vendors as $vendor)<tr><td>{{ $vendor->code }}</td><td><strong>{{ $vendor->name }}</strong></td><td><span class="badge-pill">{{ config('operations.vendor_types.'.$vendor->type, $vendor->type) }}</span></td><td>{{ $vendor->pic ?? '—' }}<br><small>{{ $vendor->email ?? $vendor->phone }}</small></td><td>@if($vendor->trashed())<span class="status-badge status-inactive">Diarsipkan</span>@elseif($vendor->is_active)<span class="status-badge status-active">Aktif</span>@else<span class="status-badge status-inactive">Nonaktif</span>@endif</td><td>@if(!$vendor->trashed())<a class="text-link" href="{{ route('vendors.edit',$vendor) }}">Lihat / Edit</a>@endif</td></tr>@empty<tr><td colspan="6"><div class="empty-state"><x-icon name="users"/><h3>Belum ada vendor yang sesuai</h3><p>Tambahkan vendor atau sesuaikan pencarian.</p></div></td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $vendors->links() }}</div></section>
@endsection