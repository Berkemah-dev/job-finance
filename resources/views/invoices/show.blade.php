@extends('layouts.app') @section('title','Detail Invoice') @section('content')
<div class="page-heading"><div><p class="eyebrow">INVOICE</p><h1>{{ $invoice->number }}</h1><p>{{ $invoice->customer_snapshot['name'] }}</p></div><a class="text-link" href="{{ route('invoices.index') }}">Kembali</a></div>
<div class="quote-actions">@if($invoice->status!=='paid')<a class="button button-primary" href="{{ route('payments.create',$invoice) }}">+ Catat pembayaran</a>@endif @if((float) $invoice->tax > 0)<a class="button button-secondary" href="{{ route('invoices.coretax',$invoice) }}">Ekspor XML Coretax</a>@endif <a class="button button-secondary" href="{{ route('jobs.show',$invoice->job) }}">Lihat job</a></div>
<section class="panel"><div class="panel-heading"><h2>Tagihan customer</h2><span class="status-badge status-{{ $invoice->status }}">{{ str_replace('_',' ',ucwords($invoice->status,'_')) }}</span></div><dl class="detail-grid"><div><dt>Tanggal invoice</dt><dd>{{ $invoice->invoice_date->format('d/m/Y') }}</dd></div><div><dt>Jatuh tempo</dt><dd>{{ $invoice->due_date->format('d/m/Y') }}</dd></div><div><dt>Job</dt><dd>{{ $invoice->job->number }}</dd></div><div><dt>Mata uang</dt><dd>{{ $invoice->currency }}</dd></div><div><dt>Kurs terhadap Rupiah</dt><dd>{{ $invoice->currency }} {{ $invoice->currency==='IDR' ? '1.000,00' : \App\Support\Money::format($invoice->exchange_rate) }}</dd></div></dl>
<div class="table-scroll"><table><thead><tr><th>Uraian</th><th>Jenis</th><th>Jumlah</th><th class="money">Harga</th><th class="money">Nilai</th></tr></thead><tbody>@foreach($invoice->items as $item)<tr><td>{{ $item->description }}</td><td>{{ ucfirst($item->type) }}</td><td>{{ \App\Support\Money::format($item->quantity) }} {{ $item->unit }}</td><td class="money">{{ \App\Support\Money::format($item->unit_price) }}</td><td class="money">{{ \App\Support\Money::format($item->amount) }}</td></tr>@endforeach</tbody></table></div><div class="summary-box"><div class="summary-row"><span>Subtotal</span><strong>Rp {{ \App\Support\Money::format($invoice->subtotal) }}</strong></div><div class="summary-row"><span>Pajak</span><strong>Rp {{ \App\Support\Money::format($invoice->tax) }}</strong></div><div class="summary-row"><span>Dibayar</span><strong>Rp {{ \App\Support\Money::format($invoice->paid_amount) }}</strong></div><div class="summary-row summary-total"><span>Sisa tagihan</span><strong>Rp {{ \App\Support\Money::format($invoice->balance) }}</strong></div></div>@if($invoice->currency!=='IDR' && (float) $invoice->exchange_rate > 0)<div class="summary-box"><div class="summary-row"><span>Subtotal setara</span><strong>{{ $invoice->currency }} {{ \App\Support\Money::format((string) $invoice->inInvoiceCurrency('subtotal')) }}</strong></div><div class="summary-row"><span>Pajak setara</span><strong>{{ $invoice->currency }} {{ \App\Support\Money::format((string) $invoice->inInvoiceCurrency('tax')) }}</strong></div><div class="summary-row summary-total"><span>Total setara</span><strong>{{ $invoice->currency }} {{ \App\Support\Money::format((string) $invoice->inInvoiceCurrency('total')) }}</strong></div></div>@endif</section>
<div class="section-heading"><h2>Riwayat pembayaran</h2></div><section class="panel"><div class="table-scroll"><table><thead><tr><th>Nomor</th><th>Tanggal</th><th>Metode</th><th>Rekening</th><th class="money">Jumlah</th></tr></thead><tbody>@forelse($invoice->payments as $payment)<tr><td>{{ $payment->number }}</td><td>{{ $payment->payment_date->format('d/m/Y') }}</td><td>{{ ucfirst($payment->method) }}</td><td>{{ $payment->account->code }} — {{ $payment->account->name }}</td><td class="money">Rp {{ \App\Support\Money::format($payment->amount) }}</td></tr>@empty<tr><td colspan="5">Belum ada pembayaran.</td></tr>@endforelse</tbody></table></div></section>
@endsection
