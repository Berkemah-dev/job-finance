@extends('layouts.app')
@section('title','Quotation')
@section('content')
<div class="page-heading"><div><p class="eyebrow">OPERASIONAL</p><h1>Quotation</h1><p>Susun penawaran, pantau persetujuan, dan mulai pekerjaan.</p></div><a class="button button-primary" href="{{ route('quotations.create') }}">+ Buat quotation</a></div>
<section class="panel"><form class="filter-bar" method="GET"><input name="search" value="{{ $search }}" placeholder="Cari nomor, judul, atau customer" aria-label="Cari quotation"><select name="status" aria-label="Status quotation"><option value="">Semua status</option>@foreach(\App\Enums\QuotationStatus::cases() as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ $status->label() }}</option>@endforeach</select><button class="button button-primary">Cari</button><a class="text-link" href="{{ route('quotations.index') }}">Reset</a></form>
<div class="table-scroll"><table><thead><tr><th>Nomor / Tanggal</th><th>Customer / Penawaran</th><th class="money">Total sebelum pajak</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($quotations as $quotation)<tr><td><strong>{{ $quotation->number }}</strong><br><small>{{ $quotation->quotation_date->format('d/m/Y') }}</small></td><td>{{ $quotation->customer_snapshot['name'] }}<br><small>{{ Str::limit($quotation->subject,45) }}</small></td><td class="money">Rp {{ \App\Support\Money::format($quotation->subtotal) }}</td><td><span class="status-badge status-{{ $quotation->status->value }}">{{ $quotation->status->label() }}</span></td><td><a class="text-link" href="{{ route('quotations.show',$quotation) }}">Lihat detail</a></td></tr>@empty<tr><td colspan="5"><div class="empty-state"><x-icon name="file"/><h3>Belum ada quotation yang sesuai</h3><p>Buat penawaran pertama untuk customer Anda.</p></div></td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $quotations->links() }}</div></section>
@endsection
